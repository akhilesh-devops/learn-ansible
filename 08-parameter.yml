- name: Demo for AWS Parameter store
  hosts: localhost
  connection: local
  tasks:
    - name: fetch some value
      ansible.builtin.debug:
        msg: "{{ lookup('amazon.aws.aws_ssm', 'cart.dev.CATALOGUE_HOST', region='us-east-1') }}"